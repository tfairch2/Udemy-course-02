#include <iostream>
//Manipulating binary search trees.
//  - made up of nodes:  root node, parent nodes and child nodes
//  - can only have two child nodes to a parent node in a binary search tree
using namespace std;

class Node {
    public:
       string data;
       Node *left;
       Node *right;
       Node(string d) {
           data = d;
           left = NULL;
           right = NULL:
       }
};

int main()
{

    return 0;
}